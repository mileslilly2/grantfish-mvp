generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Organization {
  id          String   @id @default(uuid())
  name        String
  entityType  String
  mission     String
  geographies String
  focusAreas  String
  taxStatus   String
  createdAt   DateTime @default(now())
}
